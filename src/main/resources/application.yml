spring:
  datasource:
    url: jdbc:mysql//localhost:3306/test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
